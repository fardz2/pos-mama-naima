name: poin_of_sale_mama_naima
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  get: 4.6.6
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.11
  google_fonts: ^6.2.1
  image_picker: ^1.1.2
  simple_barcode_scanner: ^0.1.1
  flutter_barcode_scanner: ^2.0.0
  iconsax_plus: ^1.0.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  flutter_dotenv: ^5.1.0
  badges: ^3.1.2

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
